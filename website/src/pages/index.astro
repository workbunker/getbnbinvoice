---
import Layout from "../layouts/Layout.astro";
---

<Layout
  title="GetBnBInvoice — Download Airbnb VAT Invoices Automatically"
  description="Chrome extension for EU Airbnb hosts. Bulk download your VAT invoices as PDF in seconds. Supports all EU countries."
>
  <!-- Navbar -->
  <nav class="navbar">
    <div class="nav-inner">
      <a href="/" class="nav-brand">
        <svg class="nav-logo" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
          <polyline points="14 2 14 8 20 8" />
          <line x1="16" y1="13" x2="8" y2="13" />
          <line x1="16" y1="17" x2="8" y2="17" />
        </svg>
        <span>GetBnBInvoice</span>
      </a>
      <div class="nav-links">
        <a href="#how-it-works">How It Works</a>
        <a href="#pricing">Pricing</a>
        <a href="https://chromewebstore.google.com" target="_blank" rel="noopener">Chrome Store</a>
        <a href="#pricing" class="nav-cta">Get License</a>
      </div>
      <button class="nav-toggle" aria-label="Toggle menu">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <line x1="3" y1="6" x2="21" y2="6" />
          <line x1="3" y1="12" x2="21" y2="12" />
          <line x1="3" y1="18" x2="21" y2="18" />
        </svg>
      </button>
    </div>
  </nav>

  <!-- Hero -->
  <section class="hero">
    <div class="hero-inner">
      <div class="hero-text">
        <h1>
          Download Your Airbnb Host VAT Invoices
          <span class="hero-highlight">Automatically</span>
        </h1>
        <p class="hero-sub">
          Stop wasting hours downloading invoices one by one. Our Chrome extension bulk-downloads your VAT invoices as
          PDF in seconds. Supports all EU countries.
        </p>
        <div class="hero-actions">
          <a href="#pricing" class="btn btn-primary btn-lg">Get Started</a>
          <a href="#how-it-works" class="btn btn-outline btn-lg">See How It Works</a>
        </div>
        <div class="hero-badge">
          <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <polyline points="20 6 9 17 4 12" />
          </svg>
          <span>Available in the Chrome Web Store</span>
        </div>
      </div>
      <div class="hero-visual">
        <div class="invoice-mockup">
          <div class="mockup-header">
            <div class="mockup-dot"></div>
            <div class="mockup-dot"></div>
            <div class="mockup-dot"></div>
          </div>
          <div class="mockup-body">
            <div class="mockup-row mockup-title">
              <span>Invoice</span>
              <span class="mockup-badge">PDF</span>
            </div>
            <div class="mockup-divider"></div>
            <div class="mockup-row">
              <span class="mockup-label">Invoice from</span>
              <span class="mockup-label">Date</span>
            </div>
            <div class="mockup-row">
              <span class="mockup-value">Airbnb Ireland UC</span>
              <span class="mockup-value">2026-02-15</span>
            </div>
            <div class="mockup-divider"></div>
            <div class="mockup-row mockup-label">Description</div>
            <div class="mockup-row mockup-value">Online Platform Fees for HMXXXXXXX</div>
            <div class="mockup-divider"></div>
            <table class="mockup-table">
              <thead>
                <tr>
                  <th>VAT Country</th>
                  <th>VAT Rate</th>
                  <th>Base Fee</th>
                  <th>Total</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td>PT</td>
                  <td>6.0%</td>
                  <td>&euro;150.00</td>
                  <td>&euro;159.00</td>
                </tr>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- How It Works -->
  <section id="how-it-works" class="section section-alt">
    <div class="section-inner">
      <h2 class="section-title">How It Works</h2>
      <p class="section-subtitle">Get your invoices in 4 simple steps</p>
      <div class="steps-grid">
        <div class="step">
          <div class="step-number">1</div>
          <h3>Install the Extension</h3>
          <p>Download GetBnBInvoice from the Chrome Web Store. It takes just a few seconds.</p>
        </div>
        <div class="step">
          <div class="step-number">2</div>
          <h3>Activate Your Key</h3>
          <p>Purchase a license and enter your key in the extension popup to activate it.</p>
        </div>
        <div class="step">
          <div class="step-number">3</div>
          <h3>Go to Reservations</h3>
          <p>Navigate to your Airbnb hosting reservations page. The extension detects them automatically.</p>
        </div>
        <div class="step">
          <div class="step-number">4</div>
          <h3>Download Invoices</h3>
          <p>Select the reservations you want, click download, and watch the magic happen.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Features -->
  <section class="section">
    <div class="section-inner">
      <h2 class="section-title">Built for EU Hosts</h2>
      <p class="section-subtitle">Everything you need to manage your Airbnb VAT invoices</p>
      <div class="features-grid">
        <div class="feature">
          <div class="feature-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" />
              <polyline points="7 10 12 15 17 10" />
              <line x1="12" y1="15" x2="12" y2="3" />
            </svg>
          </div>
          <h3>Batch Download</h3>
          <p>Download up to 25 invoices at once. No more clicking one by one through each reservation.</p>
        </div>
        <div class="feature">
          <div class="feature-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
              <polyline points="14 2 14 8 20 8" />
              <line x1="16" y1="13" x2="8" y2="13" />
              <line x1="16" y1="17" x2="8" y2="17" />
            </svg>
          </div>
          <h3>Full Invoice Support</h3>
          <p>Handles credit notes and multiple invoices per reservation. Get every document you need.</p>
        </div>
        <div class="feature">
          <div class="feature-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10" />
              <line x1="2" y1="12" x2="22" y2="12" />
              <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z" />
            </svg>
          </div>
          <h3>All EU Countries</h3>
          <p>Works with every EU Airbnb domain. Austria, Portugal, Germany, France, Spain, and more.</p>
        </div>
        <div class="feature">
          <div class="feature-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <rect x="3" y="11" width="18" height="11" rx="2" ry="2" />
              <path d="M7 11V7a5 5 0 0 1 10 0v4" />
            </svg>
          </div>
          <h3>Private &amp; Secure</h3>
          <p>No data leaves your browser. Everything runs locally using your existing Airbnb session.</p>
        </div>
        <div class="feature">
          <div class="feature-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10" />
              <polyline points="12 6 12 12 16 14" />
            </svg>
          </div>
          <h3>Real-Time Progress</h3>
          <p>See download progress for each invoice with the ability to cancel anytime.</p>
        </div>
        <div class="feature">
          <div class="feature-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <path d="M12 2v20M2 12h20" />
            </svg>
          </div>
          <h3>14 Free Credits</h3>
          <p>Try it before you buy. Register and get 14 free invoice downloads to test the extension.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Pricing -->
  <section id="pricing" class="section section-alt">
    <div class="section-inner">
      <h2 class="section-title">Get a License</h2>
      <p class="section-subtitle">
        Buy a license or test it with 14 free invoices first.
        <br />One credit = one invoice download.
      </p>
      <div class="pricing-grid">
        <div class="pricing-card">
          <div class="pricing-name">Starter</div>
          <div class="pricing-credits">50 Invoices</div>
          <div class="pricing-price">&euro;5</div>
          <p class="pricing-desc">For occasional hosts and small property owners.</p>
          <a href="#" class="btn btn-primary btn-block">Buy Starter</a>
        </div>
        <div class="pricing-card pricing-popular">
          <div class="pricing-badge-pop">Most Popular</div>
          <div class="pricing-name">Host</div>
          <div class="pricing-credits">200 Invoices</div>
          <div class="pricing-price">&euro;15</div>
          <p class="pricing-desc">For regular hosts with multiple listings.</p>
          <a href="#" class="btn btn-primary btn-block">Buy Host</a>
        </div>
        <div class="pricing-card">
          <div class="pricing-name">Pro</div>
          <div class="pricing-credits">500 Invoices</div>
          <div class="pricing-price">&euro;29</div>
          <p class="pricing-desc">For busy hosts and small property managers.</p>
          <a href="#" class="btn btn-primary btn-block">Buy Pro</a>
        </div>
        <div class="pricing-card">
          <div class="pricing-name">Agency</div>
          <div class="pricing-credits">2,000 Invoices</div>
          <div class="pricing-price">&euro;89</div>
          <p class="pricing-desc">For property managers and agencies.</p>
          <a href="#" class="btn btn-primary btn-block">Buy Agency</a>
        </div>
      </div>
      <p class="pricing-note">All prices include local VAT. You may add your EU VAT ID on checkout.</p>
    </div>
  </section>

  <!-- FAQ -->
  <section id="faq" class="section">
    <div class="section-inner section-narrow">
      <h2 class="section-title">Frequently Asked Questions</h2>
      <div class="faq-list">
        <details class="faq-item">
          <summary>What EU countries are supported?</summary>
          <p>
            All EU member states: Austria, Belgium, Bulgaria, Croatia, Czech Republic, Denmark, Estonia, Finland,
            France, Germany, Greece, Hungary, Ireland, Italy, Latvia, Lithuania, Luxembourg, Malta, Netherlands, Poland,
            Portugal, Romania, Slovakia, Slovenia, Spain, and Sweden.
          </p>
        </details>
        <details class="faq-item">
          <summary>How do credits work?</summary>
          <p>
            Each credit lets you download one invoice. If a reservation has multiple invoices (e.g. a main invoice and a
            credit note), each counts as one credit. Credits never expire.
          </p>
        </details>
        <details class="faq-item">
          <summary>Do I need to be logged into Airbnb?</summary>
          <p>
            Yes. The extension works with your existing Airbnb session in Chrome. Just make sure you're logged in and on
            your hosting reservations page.
          </p>
        </details>
        <details class="faq-item">
          <summary>Is my data safe?</summary>
          <p>
            Absolutely. The extension runs entirely in your browser. No reservation data, invoices, or personal
            information is ever sent to our servers. We only verify your license key.
          </p>
        </details>
        <details class="faq-item">
          <summary>Can I get a refund?</summary>
          <p>
            Yes. If you're not happy with the extension, contact us within 14 days of purchase for a full refund.
          </p>
        </details>
        <details class="faq-item">
          <summary>How do I get my free trial?</summary>
          <p>
            Register with your email on the extension and you'll receive 14 free credits to test the service before
            purchasing.
          </p>
        </details>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="footer">
    <div class="footer-inner">
      <div class="footer-brand">
        <svg class="footer-logo" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
          <polyline points="14 2 14 8 20 8" />
          <line x1="16" y1="13" x2="8" y2="13" />
          <line x1="16" y1="17" x2="8" y2="17" />
        </svg>
        <span>GetBnBInvoice</span>
      </div>
      <div class="footer-links">
        <a href="/privacy">Privacy Policy</a>
        <a href="/terms">Terms of Service</a>
        <a href="mailto:support@getbnbinvoice.com">Contact</a>
      </div>
      <p class="footer-disclaimer">
        Airbnb and the Airbnb logo are trademarks of Airbnb, Inc. GetBnBInvoice is not affiliated with or endorsed by
        Airbnb.
      </p>
    </div>
  </footer>
</Layout>

<style>
  /* ---- Navbar ---- */
  .navbar {
    position: sticky;
    top: 0;
    z-index: 100;
    background: var(--white);
    border-bottom: 1px solid var(--gray-200);
  }

  .nav-inner {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 24px;
    height: 64px;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .nav-brand {
    display: flex;
    align-items: center;
    gap: 8px;
    font-weight: 700;
    font-size: 18px;
    color: var(--green-600);
  }

  .nav-logo {
    width: 24px;
    height: 24px;
  }

  .nav-links {
    display: flex;
    align-items: center;
    gap: 32px;
    font-size: 14px;
    font-weight: 500;
    color: var(--gray-600);
  }

  .nav-links a:hover {
    color: var(--green-600);
  }

  .nav-cta {
    padding: 8px 20px;
    background: var(--green-600);
    color: var(--white) !important;
    border-radius: 8px;
    font-weight: 600;
    transition: background 0.2s;
  }

  .nav-cta:hover {
    background: var(--green-700);
  }

  .nav-toggle {
    display: none;
    background: none;
    border: none;
    cursor: pointer;
    width: 28px;
    height: 28px;
    color: var(--gray-800);
  }

  /* ---- Hero ---- */
  .hero {
    background: linear-gradient(135deg, #f0faf0 0%, var(--white) 100%);
    padding: 80px 24px;
  }

  .hero-inner {
    max-width: var(--max-width);
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 64px;
    align-items: center;
  }

  h1 {
    font-size: 48px;
    font-weight: 800;
    line-height: 1.1;
    letter-spacing: -1px;
    color: var(--gray-800);
  }

  .hero-highlight {
    color: var(--green-600);
    display: block;
  }

  .hero-sub {
    margin-top: 20px;
    font-size: 18px;
    color: var(--gray-600);
    line-height: 1.6;
  }

  .hero-actions {
    margin-top: 32px;
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
  }

  .hero-badge {
    margin-top: 24px;
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 14px;
    color: var(--gray-400);
  }

  .hero-badge svg {
    color: var(--green-600);
  }

  /* ---- Invoice Mockup ---- */
  .hero-visual {
    display: flex;
    justify-content: center;
  }

  .invoice-mockup {
    width: 100%;
    max-width: 420px;
    background: var(--white);
    border-radius: 12px;
    box-shadow:
      0 4px 24px rgba(0, 0, 0, 0.08),
      0 1px 4px rgba(0, 0, 0, 0.04);
    overflow: hidden;
    border: 1px solid var(--gray-200);
  }

  .mockup-header {
    display: flex;
    gap: 6px;
    padding: 12px 16px;
    background: var(--gray-50);
    border-bottom: 1px solid var(--gray-200);
  }

  .mockup-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--gray-200);
  }

  .mockup-body {
    padding: 20px;
    font-size: 13px;
  }

  .mockup-row {
    display: flex;
    justify-content: space-between;
    margin-bottom: 4px;
  }

  .mockup-title {
    font-size: 18px;
    font-weight: 700;
    align-items: center;
    margin-bottom: 12px;
  }

  .mockup-badge {
    background: var(--green-50);
    color: var(--green-600);
    font-size: 11px;
    font-weight: 700;
    padding: 2px 8px;
    border-radius: 4px;
  }

  .mockup-divider {
    height: 1px;
    background: var(--gray-200);
    margin: 12px 0;
  }

  .mockup-label {
    color: var(--gray-400);
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .mockup-value {
    font-weight: 500;
    color: var(--gray-800);
  }

  .mockup-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 12px;
    margin-top: 8px;
  }

  .mockup-table th {
    text-align: left;
    font-weight: 600;
    color: var(--gray-400);
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    padding: 8px 4px;
    border-bottom: 1px solid var(--gray-200);
  }

  .mockup-table td {
    padding: 10px 4px;
    font-weight: 500;
  }

  /* ---- Buttons ---- */
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    font-weight: 600;
    font-size: 15px;
    border-radius: 10px;
    padding: 12px 28px;
    transition:
      background 0.2s,
      transform 0.15s,
      box-shadow 0.2s;
    cursor: pointer;
    border: none;
    text-decoration: none;
  }

  .btn-primary {
    background: linear-gradient(135deg, var(--green-600), var(--green-700));
    color: var(--white);
    box-shadow: 0 2px 8px rgba(0, 138, 5, 0.2);
  }

  .btn-primary:hover {
    background: linear-gradient(135deg, var(--green-700), #005a03);
    box-shadow: 0 4px 12px rgba(0, 138, 5, 0.3);
    transform: translateY(-1px);
  }

  .btn-outline {
    background: var(--white);
    color: var(--green-600);
    border: 2px solid var(--green-600);
  }

  .btn-outline:hover {
    background: var(--green-50);
  }

  .btn-lg {
    padding: 14px 32px;
    font-size: 16px;
  }

  .btn-block {
    width: 100%;
  }

  /* ---- Sections ---- */
  .section {
    padding: 80px 24px;
  }

  .section-alt {
    background: var(--gray-50);
  }

  .section-inner {
    max-width: var(--max-width);
    margin: 0 auto;
  }

  .section-narrow {
    max-width: 720px;
  }

  .section-title {
    font-size: 36px;
    font-weight: 800;
    text-align: center;
    letter-spacing: -0.5px;
  }

  .section-subtitle {
    text-align: center;
    color: var(--gray-600);
    font-size: 16px;
    margin-top: 12px;
    margin-bottom: 48px;
  }

  /* ---- Steps ---- */
  .steps-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 32px;
  }

  .step {
    text-align: center;
    padding: 24px 16px;
  }

  .step-number {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: var(--green-600);
    color: var(--white);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 20px;
    font-weight: 700;
    margin: 0 auto 16px;
  }

  .step h3 {
    font-size: 16px;
    font-weight: 700;
    margin-bottom: 8px;
  }

  .step p {
    font-size: 14px;
    color: var(--gray-600);
    line-height: 1.5;
  }

  /* ---- Features ---- */
  .features-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
  }

  .feature {
    padding: 28px 24px;
    background: var(--gray-50);
    border-radius: 12px;
    border: 1px solid var(--gray-200);
    transition: box-shadow 0.2s;
  }

  .feature:hover {
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.06);
  }

  .feature-icon {
    width: 44px;
    height: 44px;
    border-radius: 10px;
    background: var(--green-50);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 16px;
  }

  .feature-icon svg {
    width: 22px;
    height: 22px;
    color: var(--green-600);
  }

  .feature h3 {
    font-size: 16px;
    font-weight: 700;
    margin-bottom: 8px;
  }

  .feature p {
    font-size: 14px;
    color: var(--gray-600);
    line-height: 1.5;
  }

  /* ---- Pricing ---- */
  .pricing-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
  }

  .pricing-card {
    position: relative;
    background: var(--white);
    border-radius: 16px;
    padding: 32px 24px;
    text-align: center;
    border: 1px solid var(--gray-200);
    box-shadow: 0 1px 4px rgba(0, 0, 0, 0.04);
    display: flex;
    flex-direction: column;
    transition: box-shadow 0.2s;
  }

  .pricing-card:hover {
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.08);
  }

  .pricing-popular {
    border-color: var(--green-600);
    box-shadow: 0 4px 16px rgba(0, 138, 5, 0.12);
  }

  .pricing-badge-pop {
    position: absolute;
    top: -12px;
    left: 50%;
    transform: translateX(-50%);
    background: var(--green-600);
    color: var(--white);
    font-size: 12px;
    font-weight: 700;
    padding: 4px 16px;
    border-radius: 20px;
    white-space: nowrap;
  }

  .pricing-name {
    font-size: 14px;
    font-weight: 600;
    color: var(--gray-400);
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-bottom: 8px;
  }

  .pricing-credits {
    font-size: 22px;
    font-weight: 800;
    color: var(--gray-800);
    margin-bottom: 8px;
  }

  .pricing-price {
    font-size: 40px;
    font-weight: 800;
    color: var(--green-600);
    margin-bottom: 12px;
  }

  .pricing-desc {
    font-size: 14px;
    color: var(--gray-600);
    margin-bottom: 24px;
    flex: 1;
  }

  .pricing-note {
    text-align: center;
    font-size: 13px;
    color: var(--gray-400);
    margin-top: 24px;
  }

  /* ---- FAQ ---- */
  .faq-list {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .faq-item {
    background: var(--gray-50);
    border: 1px solid var(--gray-200);
    border-radius: 12px;
    overflow: hidden;
  }

  .faq-item summary {
    padding: 18px 24px;
    font-weight: 600;
    font-size: 15px;
    cursor: pointer;
    list-style: none;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .faq-item summary::after {
    content: "+";
    font-size: 20px;
    font-weight: 300;
    color: var(--gray-400);
    transition: transform 0.2s;
  }

  .faq-item[open] summary::after {
    content: "-";
  }

  .faq-item summary::-webkit-details-marker {
    display: none;
  }

  .faq-item p {
    padding: 0 24px 18px;
    font-size: 14px;
    color: var(--gray-600);
    line-height: 1.6;
  }

  /* ---- Footer ---- */
  .footer {
    background: var(--gray-800);
    color: var(--gray-400);
    padding: 40px 24px;
  }

  .footer-inner {
    max-width: var(--max-width);
    margin: 0 auto;
    text-align: center;
  }

  .footer-brand {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    font-weight: 700;
    font-size: 18px;
    color: var(--white);
    margin-bottom: 16px;
  }

  .footer-logo {
    width: 22px;
    height: 22px;
  }

  .footer-links {
    display: flex;
    justify-content: center;
    gap: 24px;
    font-size: 14px;
    margin-bottom: 20px;
  }

  .footer-links a:hover {
    color: var(--white);
  }

  .footer-disclaimer {
    font-size: 12px;
    color: var(--gray-400);
    opacity: 0.7;
    max-width: 500px;
    margin: 0 auto;
    line-height: 1.5;
  }

  /* ---- Mobile ---- */
  @media (max-width: 900px) {
    .nav-links {
      display: none;
    }

    .nav-toggle {
      display: block;
    }

    .hero-inner {
      grid-template-columns: 1fr;
      gap: 40px;
      text-align: center;
    }

    h1 {
      font-size: 36px;
    }

    .hero-actions {
      justify-content: center;
    }

    .hero-badge {
      justify-content: center;
    }

    .steps-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .features-grid {
      grid-template-columns: 1fr 1fr;
    }

    .pricing-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 600px) {
    .hero {
      padding: 48px 20px;
    }

    h1 {
      font-size: 28px;
    }

    .hero-sub {
      font-size: 16px;
    }

    .section {
      padding: 56px 20px;
    }

    .section-title {
      font-size: 28px;
    }

    .steps-grid {
      grid-template-columns: 1fr;
    }

    .features-grid {
      grid-template-columns: 1fr;
    }

    .pricing-grid {
      grid-template-columns: 1fr;
    }

    .hero-actions {
      flex-direction: column;
      align-items: stretch;
    }
  }
</style>
